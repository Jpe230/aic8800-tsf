# On this branch the packaging's kernel-compatibility patches are already
# applied as commits, so the series is intentionally empty here. This branch
# exists for direct kbuild:
#   make -C /lib/modules/$(uname -r)/build M=$PWD/src/SDIO/driver_fw/driver/aic8800 -j4 modules
# The Debian packaging flow should stay on the upstream tag, where this series
# is still applied at build time.
